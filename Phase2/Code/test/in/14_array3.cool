class Par {
	let int length;
}

class Child{
	let int value;
	let Par par;
}

class Main {
	
    func main() int {
		let Child[] list;
		let Par parent;
		let int lengthOfList;
		parent = list.par;
		lengthOfList = parent.length;
		list = new Child[lengthOfList];
		print(list[32]);
        	return 0;
    }
}
